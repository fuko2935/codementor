/**
 * This module was removed as part of SPEC-AUTH-REMOVE-001.
 *
 * The MCP server no longer provides a built-in authentication context or
 * scope-based authorization mechanism.
 *
 * All remaining imports of `authContext` must be removed from the codebase.
 * For any authentication/authorization needs, rely on external infrastructure
 * such as reverse proxies, mTLS, IP allowlists, or dedicated API gateways.
 */
export {};/**
 * This module was removed as part of SPEC-AUTH-REMOVE-001.
 *
 * The MCP server no longer provides a built-in authentication context or
 * scope-based authorization mechanism.
 *
 * All remaining imports of `authContext` must be removed from the codebase.
 * For any authentication/authorization needs, rely on external infrastructure
 * such as reverse proxies, mTLS, IP allowlists, or dedicated API gateways.
 */
export {};