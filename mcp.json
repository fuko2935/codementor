{
  "mcpServers": {
    "gemini-mcp-local": {
      "command": "npx",
      "args": ["gemini-mcp-local"],
      "env": {
        "MCP_LOG_LEVEL": "debug",
        "MCP_TRANSPORT_TYPE": "http",
        "MCP_HTTP_PORT": "3010"
      }
    }
  }
}
