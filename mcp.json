{
  "mcpServers": {
    "codementor": {
      "command": "npx",
      "args": ["codementor"],
      "env": {
        "MCP_LOG_LEVEL": "debug",
        "MCP_TRANSPORT_TYPE": "http",
        "MCP_HTTP_PORT": "3010"
      }
    }
  }
}
